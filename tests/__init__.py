"""Suite de tests de wacloud.

Es un paquete para que ``from tests.factories import ...`` funcione por import
normal y no por tener el directorio actual en ``sys.path``, que depende de desde
dónde se lance pytest.
"""
